import React from "react";
import { AboutDiv } from "../components/AuthForm";
function About(props) {
    return (
        <AboutDiv>
            <p>Contract Inbox enables effecient online contract processing.</p>
            <p>Contract Inbox offers online contract signing with ease, speed, and security. Contract Inbox allows to manage the entire lifecycle of a contract: execution, approvals/signitures, tracking,  and accessibility all in one secure cloud space. </p>
        </AboutDiv>
    );
}

export default About;